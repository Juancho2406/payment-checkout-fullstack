{
  "name": "payment-checkout-fullstack",
  "private": true,
  "description": "Checkout kata: React SPA + hexagonal NestJS API + AWS CDK",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "ci": "pnpm install --frozen-lockfile && pnpm -r run --if-present lint && pnpm -r run --if-present test",
    "sync:projects": "node backlog/sync-projects.mjs"
  }
}
