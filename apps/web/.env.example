# Vite API prefix. Leave as /api/v1 so the dev proxy forwards to Nest (:3001).
VITE_API_BASE_URL=/api/v1

# PSP sandbox — public key only. The private key stays on the API.
VITE_PSP_BASE_URL=
VITE_PSP_PUBLIC_KEY=
